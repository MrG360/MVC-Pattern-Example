# MVC-Pattern-Example

MVCPattern is the main file where the whole application works.

1. StudentController is the controller which handles the request from MVCPattern.java file.
2. Student is the model class which handles the data and perform CRUD operations in it.
3. StudentView is the class which provides the view to the model recieved through the controller.
